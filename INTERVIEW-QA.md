# 项目面试问答（脱敏版）

> 用途：面试前复习与模拟问答。已去掉仓库地址、提交记录、日期、具体路径等细节；数字均来自项目内可复现的基准实验，口径与 `README-RESUME.md` 一致。

---

## 一、项目整体介绍（一分钟疏通版）

这是一个「Prompt 内容平台 + AI 优化工作流」的后端系统，整体分两层：

- **Java 主服务**（Spring Boot 3 / Java 21）：负责用户与鉴权、Prompt 的创建-审核-发布-检索全流程、点赞收藏浏览等互动计数与热度排行、AI 优化工作流的编排与持久化，以及 SSE 流式输出。
- **AI 服务**（FastAPI）：负责真正吃算力的部分——RAG 混合检索、Prompt 分析与优化 Agent、流式生成，Java 侧通过 HTTP 网关调用。

两条业务闭环：① 用户发布 Prompt → 走责任链审核（敏感词/质量/原创性，异步执行）→ 通过后上架；② 用户提交优化请求 → AI 分析 → 优化 → 复核评分 → 结果可确认保存为新 Prompt。外围是互动计数（Redis 实时 + 消息队列延迟合并回库）和热度排行。

工程上做了几件贯穿全局的事：把锁/限流/幂等/布隆等横切能力沉淀成了内部 Starter；把行为日志/通知从进程内异步解耦到了自研的轻量消息组件（可开关切换）；补齐了 OpenTelemetry/Prometheus/Grafana 可观测；并且所有关键路径都有基准数据佐证取舍。

**一句话概括**：不是玩具 CRUD——每一条业务链路都能讲出「为什么这么设计 + 怎么实现的 + 数据上得到了什么」。

---

## 二、分点问答

### 1. 互动计数链路：Redis 异步合并写

**【为什么】**
点赞/收藏/浏览/复制是高频写操作。直接更新数据库单行（`UPDATE ... SET count = count + 1`）在压测下只有约 180 次/秒，热点 Prompt 的行锁会迅速成为瓶颈，而且这类计数的实时性要求远低于资金类数据，不值得每次互动都打一次数据库。

**【怎么做】**
- 计数先写 Redis（INCR/ZINCRBY），详情页读取时把 Redis 增量与库内基准值合并展示；
- 标记"脏数据"集合，通过消息队列的**延迟队列**在秒级后合并回刷数据库（同一条 Prompt 短时间内的多次互动只产生一次回刷）；
- 回刷前用 Lua 快照取走增量、再用 Lua 原子扣减，配合分布式锁保证多实例下不会重复回刷；
- 缓存过期时间加随机抖动，避免批量失效造成的雪崩。

**【好处】**
写路径从数据库行锁竞争变成 Redis 单键原子操作；数据库写入次数与互动次数解耦（合并后大幅下降）；详情读取始终是缓存 + 内存计算，不会读到中间态。

**【产出】**
- 微基准：Redis INCR 约 914 次/秒，是直写数据库（181 次/秒）的约 **5 倍**；
- 全链路 A/B（20/50 并发，Redis 合并写 vs 直写数据库）：合并写模式吞吐更高、数据库写入次数显著减少；
- 并发拐点实验：详情接口在 100 并发达到峰值约 230 TPS，400 并发塌陷至一半——定位出瓶颈不在业务代码而在连接池配置（Redis 池 8、数据库池 20），并据此给出扩容与合并写两个优化方向（其中本地缓存已落地，见第 4 点）。

### 2. 分布式锁（内部组件）

**【为什么】**
计数回刷、缓存重建这类路径存在"多实例同时执行"的风险。原来的做法是 SETNX + UUID 释放，有两个问题：不可重入（同线程内再次获取会自锁）、没有续期（持锁超过过期时间后锁会失效，并发再次发生）。

**【怎么做】**
- 用 Redis Hash 存 token + 计数实现**可重入**：同一线程重复获取时计数 +1，释放时 -1，归零才删锁；
- 释放与续期都用 Lua 脚本，先校验持有者 token 再操作，杜绝"A 的锁被 B 删掉"；
- 增加看门狗线程，以租约 1/3 的间隔自动续期，业务执行多久锁就活多久；
- 全部通过内部 Starter 自动装配，业务侧一个 Bean 注入即可用。

**【好处】**
长任务不会因锁过期而失去互斥；误删风险被 token 校验消除；接入成本低（零 XML、零手动脚本管理）。

**【产出】**
- 集成测试（打真实 Redis）：20 个线程竞争同一把锁时全部失败、互斥成立；300ms 短租约在持锁 1 秒后依然存活（看门狗生效）；
- 微基准：无竞争下获取+释放约 361 对/秒，4 线程竞争组总吞吐约 2167 次/秒，未见线程饥饿。

### 3. 限流升级：滑动窗口替代固定窗口

**【为什么】**
原有限流是"计数 + 到期重置"的固定窗口：窗口边界处可能出现瞬时两倍流量（前一窗口末尾 + 后一窗口开头），对登录、搜索这类接口的保护不够精确。

**【怎么做】**
- 用 ZSET 实现滑动窗口：每次请求以当前时间戳为 score 写入成员，判定前先清理窗口外的过期成员，再按窗口内成员数比较限额——**全部在一条 Lua 脚本里原子完成**，多实例共享同一窗口；
- 封装成注解（`@RateLimit`），支持按 IP、登录用户、SpEL 表达式三种维度；
- Redis 故障时默认放行（可用性优先），并在指标里记录拒绝数。

**【好处】**
窗口边界突刺被消除；限流判定原子化，无竞态；新接口加限流只需一行注解。

**【产出】**
- 微基准：滑动窗口约 728 次/秒 vs 固定窗口 914 次/秒，**以约 20% 的吞吐代价换窗口语义精确**（限流本身是低 QPS 前台逻辑，代价可接受）；
- 并发突刺测试：50 并发同时请求时恰好放行限额数，原子性得到验证；
- 登录、搜索限流已切换为新实现，复制接口新增注解限流。

### 4. 缓存三防 + 两级缓存

**【为什么】**
三个经典问题同时存在：查询不存在的 ID 每次都打到数据库（穿透）；热点详情缓存失效瞬间大量并发回源（击穿）；批量缓存同时过期（雪崩）。另外，压测发现即使命中 Redis 缓存，每次请求仍要走多次 Redis 往返，连接池先于业务饱和。

**【怎么做】**
- **穿透**：自研 Redis bitmap 布隆过滤器（位数组长度与哈希函数个数按期望元素数和误判率推导，k 次位操作 pipeline 合并为一次往返）+ 启动预热存量 ID + 空值短缓存双保险；
- **击穿**：缓存未命中时用分布式锁做"单飞"重建——同一时刻只有一个请求回源数据库，其余等锁失败后二次读缓存；
- **雪崩**：所有缓存 TTL 加随机抖动；
- **两级缓存**：热点详情加一层进程内本地缓存（容量上限 + 短 TTL），计数与点赞状态仍然每次实时合并，保证本地缓存只缓存静态内容；更新/删除时与 Redis 缓存同点失效。

**【好处】**
无效请求在 Redis 之前就被拦截；回源数据库的并发被压到 1；热点请求的网络往返从"多次 Redis RTT"降为"进程内纳秒级命中"。

**【产出】**
- 布隆预热实测：存量 1500 条数据约 4 秒、位图约 117KB；误判率实测在理论值 2 倍裕量内；
- 微基准：本地缓存命中约 51ns，Redis 命中约 614µs，差约 **12000 倍**；
- 端到端复测：加本地缓存后，详情接口 100 并发 TPS 从约 230 提升到约 354（**+54%**），P50 延迟从 270ms 降到 163ms。

### 5. 轻量消息组件（项目内实现）

**【为什么】**
行为日志、通知这类链路与主业务耦合在进程内异步线程里，无法独立演进，也不便压测观察；直接改 RabbitMQ 主链路（审核、计数回刷）风险又太高。需要一个能自己掌控、可开关接入的中间层。

**【怎么做】**
从零实现了一个消息组件，三层结构：
- **存储引擎**：分段 append-only 日志文件（4 字节长度前缀 + 载荷），顺序写、段滚动、启动时重建索引、尾部损坏记录自动截断；每条消息落盘（fsync）；
- **消费语义**：topic + 消费组独立游标，pull 模型；ack 才推进游标（at-least-once）；nack 累计投递次数，超过上限自动转投死信队列，防止毒消息阻塞；支持延迟消息（到期时间排序 + 定时调度提升）；
- **网络层**：Netty 私有协议（魔数/版本/类型/请求 ID + 长度前缀），自行处理半包粘包，客户端请求 ID + Future 表支持单连接并发请求。

同时封装了 Spring Boot Starter：模板类 + 注解式消费者 + 健康检查，**默认关闭**，通过开关渐进接入。

**【好处】**
行为日志/通知与主业务解耦，可独立压测、独立演进；消息确认/重试/死信语义与主链路 RabbitMQ 完全对齐，切换成本低；组件默认关闭，接入失败不影响现有行为（发布失败自动回退本地处理）。

**【产出】**
- 14 项自动化测试全绿（存储恢复/段滚动/消费组隔离/重试转死信/延迟精度/端到端 Netty 链路）；
- 同机对比（单客户端 loopback）：**"确认"语义下发布吞吐约 1340 次/秒，高于 RabbitMQ 的 publisher-confirm 模式（约 734 次/秒）**；无确认模式下 RabbitMQ 更快（约 1.8 万次/秒），差距已定位为刷盘策略差异（本组件每条消息 fsync，RabbitMQ 该模式不逐条刷盘）——对比报告里如实写明，不回避。

### 6. 两个 Spring Boot Starter（内部沉淀 + 业务自用）

**【为什么】**
锁/限流/幂等/布隆这类横切能力散落在业务代码里，每个服务都要重写一遍；消息组件的客户端使用门槛也偏高。需要把它们沉淀成"引入即用"的组件，并且自己先用起来（吃自己的狗粮）。

**【怎么做】**
- 基础设施 Starter：自动装配 + 配置属性类 + Micrometer 指标；限流的 IP/用户维度通过 SPI 接口解耦，Starter 本身不依赖 Servlet/Security；
- 消息组件 Starter：模板类管理连接生命周期、`@MiniMqListener` 注解扫描注册消费者、执行成功自动 ack/异常自动 nack、附带健康探针；
- 两个 Starter 都由业务服务真实接入：基础设施组件直接承载计数同步锁、限流、幂等、布隆；消息组件承载行为日志与通知链路。

**【好处】**
接入成本 = 一个 Maven 依赖 + 零 Java 配置；组件与宿主框架解耦（不引入不必要依赖）；默认关闭的开关设计保证渐进替换安全。

**【产出】**
- 基础设施 Starter 20 项集成测试（全部打真实 Redis）通过；
- 消息 Starter 端到端测试通过（真实 Broker：发布 → 注解消费者收到）；
- 业务侧行为日志/通知可通过配置在"进程内"与"消息组件"间一键切换，发布失败自动降级，降级逻辑有单测覆盖。

### 7. RAG 检索质量：三通道矩阵与审计修正

**【为什么】**
检索质量直接影响生成答案的可信度，需要一个可量化、可复现的评测体系；同时发现关键词通道原有的查询解析是 AND 语义，自然语言问题几乎永远命中为空——关键词通道形同虚设。

**【怎么做】**
- 建立评测体系：人工标注语料 + 查询集（chunk 级黄金标签），统一评测脚本跑 Recall@K / MRR / 延迟；
- 关键词通道逐步修复：先改为词元 OR 语义 → 评测发现裸 OR 把高频词噪声注入融合（混合检索反而不如单路稠密检索）→ 再用语料词频统计做 **IDF 过滤**（丢弃覆盖过高的词、按 IDF 保留区分度最高的词、全被滤空时主动放弃该通道）；
- 每次改动都先复现历史基线再对比，保证前后数据可比。

**【好处】**
检索行为可解释（哪些词被过滤、为什么）；低区分度查询自动退化为稠密检索兜底；每一个结论都有数据而非感觉。

**【产出】**
- 稠密检索基线 Recall@5 = 0.574，混合（修复后）= 0.536，**IDF 过滤带来 +7.2% 但仍低于单路稠密检索——这个负结论被如实保留**，并解释了原因（该语料下词面信号不足以压过语义信号）；
- 加重排后 Recall@5 = 0.646，是唯一稳定超过单路检索的配置，代价是 CPU 重排中位约 14 秒/查询，只适合离线或低延迟不敏感场景；
- 向量索引 EXACT vs HNSW 质量持平、延迟接近，规模不足时结论如实标注；
- 压测定位检索服务吞吐饱和点（瓶颈在本地嵌入推理），为资源规划提供依据。

### 8. 工程底座

**【为什么】**
单点功能不难，难的是让整条链路在工程上"经得起追问"：状态一致性、并发正确性、可观测性、可复现性缺一不可。

**【怎么做】**
- 多模块 Maven 工程，业务/组件/基准各自独立；
- 审核与计数回刷走消息队列，事务提交后才投递，杜绝消息与数据库状态不一致；
- 幂等（重复确认不重复创建）+ 版本号乐观锁（并发更新拦截）双保险；
- OpenTelemetry 链路追踪 + Prometheus 指标 + Grafana 面板 + 统一异常处理；
- 基准全部脚本化，任何数字都能一键复现。

**【好处】**
面试时每个细节都能追溯；新人接手成本低；性能与质量问题有据可查。

**【产出】**
- 全模块自动化测试通过（业务 17 类、基础设施 20、消息组件 14、Starter 端到端 1、AI 服务 126 用例）；
- 基准报告 + 原始数据 + 复现命令全部入库，口径一致。

---

## 三、常见追问的预备回答

- **为什么不用 Redisson/RateLimiter 现成库？** 业务规模下引入重型依赖不划算；自己实现能讲清 token 校验、可重入计数、看门狗续期这些细节——面试要考的就是这些细节，而不是调库能力。
- **自研消息组件为什么不做分区/副本/共识？** 明确边界：它的定位是解决"非关键链路的解耦与可观测"，主链路仍走成熟中间件；分区副本是分布式系统的复杂度核心，如实声明未实现比假装有更可信。
- **混合检索为什么没打赢单路检索？** 这正是最有价值的结论：用数据证明了"混合不是银弹"，并给出了可解释的原因和下一步方向（候选级词频统计做真 BM25）。
- **这些数字可信吗？** 全部来自仓库内可复现的基准脚本，环境与误差区间在报告里写明了；负结果（限流 -20%、RAG 负结论）没有隐藏。
